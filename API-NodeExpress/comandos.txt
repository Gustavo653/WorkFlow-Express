Preparação MsSQLServer
sudo docker run -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=SenhaBraba123" -p 1433:1433 --name MsSQL -d mcr.microsoft.com/mssql/server:2022-latest

Preparação PostgreSQL
sudo docker run --name PostgreSQL -e POSTGRES_PASSWORD=SenhaBraba123 -d -p 5432:5432 postgres

Build imagem docker
sudo docker build -t workflow-api .
sudo docker run -p -d --name WorkFlow-API 4000:3000 workflow-api

Executar no docker swarm
sudo docker swarm init
sudo docker swarm init --advertise-addr 2804:14c:5f81:9b58::1001
sudo docker stack deploy -c docker-compose.yml workflow-api-stack

Parar docker swarm
sudo docker swarm leave --force


Parte WEB
sudo docker run -d -p 4200:80 -e API_URL=http://192.168.0.18:3000 --name workflow-web workflow-web